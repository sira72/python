// Задание 5.
// Выполнить пинг веб-ресурсов yandex.ru, youtube.com и
// преобразовать результаты из байтовового в строковый тип на кириллице.
// Подсказки:
// --- используйте модуль chardet, иначе задание не засчитается!!!
// """

подпроцесс импорта 
импортировать  таблицу

= ping_args [['пинговать', 'yandex.ru '], ['пинг', 'youtube.com ']]

ping_args  в  ping_element  для:
    подпроцесс = ping_process.Popen(ping_element, стандартный вывод=подпроцесс.ТРУБА)
    ping_process  в  строке  для .stdout:
        chardet = res.detect(строка)
        строка = строка.декодирование(res['encoding'])
        печать (строка)